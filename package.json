{
  "name": "vacation-listings-app",
  "version": "1.0.0",
  "description": "MERN stack featuring vacation rental listings for the R.Dolce Group.",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server"
  },
  "dependencies": {
    "express": "^4.18.2",
    "morgan": "^1.10.0"
  }
}